{
  "name": "@buildingos/bootstrap",
  "version": "0.1.0",
  "description": "BuildingOS first-boot bootstrap — tenant scaffold, wizard (language first → engine → model → credentials → git), injectable I/O. Shared by the CLI and the web console (single source of truth).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@buildingos/adapter-codex": "workspace:*",
    "@buildingos/adapter-dsh": "workspace:*",
    "@buildingos/normalizer": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
