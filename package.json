{
  "name": "buildingos",
  "private": true,
  "description": "BuildingOS monorepo — normalizer, engine adapters, conformance suite",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm -r build && pnpm -r typecheck && pnpm -r test",
    "verify": "pnpm check && pnpm --filter @buildingos/cli demo",
    "buildingos": "pnpm exec buildingos"
  },
  "dependencies": {
    "@buildingos/cli": "workspace:*"
  }
}
