{
  "name": "@buildingos/adapter-dsh",
  "version": "0.1.0",
  "description": "BuildingOS DSH (DeepSeek Harness) adapter — compile() renders the DSH engine view; run() bridge pending (ACP vs in-process cordis, adapter-contract §9).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@buildingos/normalizer": "workspace:*",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
