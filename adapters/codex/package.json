{
  "name": "@buildingos/adapter-codex",
  "version": "0.1.0",
  "description": "BuildingOS Codex (OpenAI Codex CLI) adapter — compile() renders the Codex engine view; run() bridge pending (codex mcp-server, experimental).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@buildingos/normalizer": "workspace:*",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
