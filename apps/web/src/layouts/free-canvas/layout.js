/**
 * 自由画布布局。
 *
 * 和「经典三栏」的区别：三栏布局把屏幕分成左右两栏，卡片按顺序堆在里面；
 * 自由画布只有一个 canvas 区域，每张卡的位置和大小由自己那份配置里的
 * box 决定（{ x, y, w, h }，单位是百分比，所以换个分辨率也不会散）。
 *
 * box 挂在卡片实例上而不是态势上，因为实例本来就是「这一页要用的那张卡」：
 * 演示模式每一页都是一份独立配置，所以同一张卡在不同页可以有不同位置。
 */
export default {
  id: 'free-canvas',
  name: '自由画布',
  regions: [
    { id: 'canvas', role: 'free', label: '自由画布' },
  ],
};
